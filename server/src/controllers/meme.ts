import * as http from 'http'
import * as url from 'url'

import { Request, Response } from 'express'
import * as MemeModel from '../models/Meme'

export class Meme {

    getById(id: number): MemeModel {
        
    }

}